name: Test1

on: [push]

jobs:
  Build:
    name: FirstBuild
    runs-on: windows-lastest

    steps:
      - name: Checkout2
        uses: actions/checkout@v4
        with:
          node-version: lastest
      - run: git checkout -b "Test2"
      - run: git checkout main
      - run: git clone UNESCOTRACKER
